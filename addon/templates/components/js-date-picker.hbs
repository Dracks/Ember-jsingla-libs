{{#each selectList as |select|}}
	{{#if select.content}}
		{{js-select content=select.content selectedValue=select.value class="form-control" action="updateValue"}}
	{{/if}}
	{{select.separator}}
{{/each}}
